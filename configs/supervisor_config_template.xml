<?xml version="1.0"?>
<nemea-supervisor>
<!-- include ${prefix}/share/nemea-supervisor/warning.sup -->

  <modules>
    <name>Data sources</name>
    <enabled>true</enabled>
    <!-- include ${prefix}/etc/data-sources -->
  </modules>

  <modules>
    <name>Detectors</name>
    <enabled>true</enabled>
    <!-- include ${prefix}/etc/detectors -->
  </modules>

  <modules>
    <name>Reporters</name>
    <enabled>true</enabled>
    <!-- include ${prefix}/etc/reporters -->
  </modules>

  <modules>
    <name>Loggers</name>
    <enabled>true</enabled>
    <!-- include ${prefix}/etc/loggers -->
  </modules>
</nemea-supervisor>
